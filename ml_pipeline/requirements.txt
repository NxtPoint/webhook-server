# ML Pipeline dependencies — install in addition to main requirements.txt
# pip install -r ml_pipeline/requirements.txt
#
# NOTE: torch + torchvision are installed separately in Dockerfile via
# --index-url https://download.pytorch.org/whl/cu121 to use system CUDA
# and avoid bundling ~2GB of redundant CUDA libs from PyPI.

--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.3.1+cu121
torchvision==0.18.1+cu121
ultralytics==8.2.50
opencv-python==4.9.0.80
ffmpeg-python==0.2.0
numpy==1.26.4
scipy==1.13.1
boto3==1.34.131
psycopg2-binary==2.9.9
mediapipe==0.10.14
tqdm==4.66.4
pandas==2.2.2
matplotlib==3.9.0
seaborn==0.13.2
sqlalchemy==2.0.31
psycopg[binary]==3.1.19
sahi==0.11.18
# ViTPose-Base for far-baseline ROI pose extraction (roi_extractors/pose.py).
# Weights are pre-downloaded at image-build time in the Dockerfile so the
# Batch job doesn't pay a 300 MB download on every cold start.
transformers==4.44.2
